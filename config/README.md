# Copaster Configuration

Copaster allows "smart" copying, which implies that some transformations will be applied to files. This module is responsible for reading and validating the configuration that defines which transformations to run.

> 🚧 Under Construction