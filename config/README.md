# Copaster Configuration

Copaster allows "smart" copying, which implies that some transformation on files will be done. To define which transformations are needed a form of configuration is required. This module is responsible for dealing with the configuration.

> 🚧 Under Construction